

<p><em>{$cart_items}</em> items in <a href="http://{$WEB_ROOT}{$PUBLIC_DIR}/store/cart">basket</a></p>
<p>&nbsp;</p>

{*
<div id="basket">
<div id="basket_inner">
{if isset($user_id)}
<p>Logged in as <em><a href="http://{$WEB_ROOT}{$PUBLIC_DIR}/store/users">{$username}</a></em>. You have <em>{$cart_items}</em> items in your <a href="http://{$WEB_ROOT}{$PUBLIC_DIR}/store/cart">shopping cart</a>.</p>
{else}
<p><a href="http://{$WEB_ROOT}{$PUBLIC_DIR}/misc/login">Log in</a> to grab a basket or <a href="http://{$WEB_ROOT}{$PUBLIC_DIR}/store/register">register</a>.</p>
{/if}
<p class="clear">&nbsp;</p>
</div>
</div>
*}